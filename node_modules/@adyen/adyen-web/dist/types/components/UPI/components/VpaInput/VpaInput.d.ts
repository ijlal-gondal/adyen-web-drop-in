import { h } from 'preact';
import './VpaInput.scss';
interface VpaInputProps {
    data?: {};
    disabled?: boolean;
    onChange({ data: VpaInputDataState, valid, errors, isValid: boolean }: {
        data: any;
        valid: any;
        errors: any;
        isValid: any;
    }): void;
    onSetInputHandlers(handlers: VpaInputHandlers): void;
}
export declare type VpaInputHandlers = {
    validateInput(): void;
};
declare const VpaInput: (props: VpaInputProps) => h.JSX.Element;
export default VpaInput;
